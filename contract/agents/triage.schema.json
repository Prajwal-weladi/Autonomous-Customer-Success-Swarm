{
  "title": "TriageAgent",
  "type": "object",
  "properties": {
    "intent": {
      "type": "string",
      "enum": [
        "refund",
        "exchange",
        "delivery_issue",
        "order_status",
        "general"
      ]
    },
    "urgency": {
      "type": "string",
      "enum": ["low", "medium", "high"]
    }
  },
  "required": ["intent", "urgency"]
}
